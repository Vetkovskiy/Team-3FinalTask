# Task Tracker

Трекер задач на Java — командный итоговый проект.

## ⚙️ Функционал
- Создание задачи
- Изменение и удаление задачи
- Отслеживание статуса и приоритета
- Просмотр списка задач

## 👥 Команда
- Ачилов Никита - многопоточность, интеграция
- Дмитрий Михалкин — паттерны
- Дмитрий Гулько — работа с файлами
- Владимир Ветковский — коллекции и стримы


## ▶️ Запуск тестов (без JUnit):
Сборка тест-классов:
javac -cp "src/main/java" -d "target/test-classes" src/test/java/com/tracker/*.java
Если ругается на AppTest (JUnit), временно не компилируйте его или удалите/закомментируйте.
Запуск:
java -cp "target/classes;target/test-classes" com.tracker.SortTests
java -cp "target/classes;target/test-classes" com.tracker.SearchTests
java -cp "target/classes;target/test-classes" com.tracker.FileParsingTests
